---
description: Build + deploy a versioned preview; print the preview URL.
---

Run `pnpm deploy:preview` and surface the resulting preview URL on its own
line so it's easy to tap on mobile. Do not auto-promote to production.
