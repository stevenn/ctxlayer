# Screenshots

These PNGs are rendered by the **Screenshots** gallery in the root
[`README.md`](../../README.md). Keep the filenames stable when refreshing.

| File | Screen (`/app/...`) | Shows |
|---|---|---|
| `admin-upstreams.png` | `admin/upstreams` | Upstream catalogue + per-tool skill/doc attachments |
| `usage.png` | `usage` | Calls / tokens, daily activity, top tools |
| `connect-upstreams.png` | `upstreams` | User self-service connect (OAuth / paste token) |
| `skills.png` | `admin/skills` | A curated skill (SKILL.md body) |
| `upstream-config.png` | `admin/upstreams` (drawer) | Transport / auth / resilience / visibility config |

## Refreshing

Sign in locally (`bun run dev` → `https://localhost:5173`) or on the live
deploy, then re-capture at ~1440px wide, 100% zoom, one consistent colour
scheme, trimmed to the app content. Overwrite the file of the same name — no
README change needed.
