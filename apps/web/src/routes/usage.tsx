export function Usage() {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Your usage</h2>
      <p style={{ color: 'var(--muted)' }}>
        Personal stats (calls, tokens, top tools) arrive in M6 once the usage pipeline lands.
      </p>
    </div>
  )
}
