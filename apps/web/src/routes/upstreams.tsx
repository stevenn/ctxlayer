export function Upstreams() {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Connect upstreams</h2>
      <p style={{ color: 'var(--muted)' }}>
        Per-upstream connect cards (paste bearer, OAuth popup) arrive in M4.
      </p>
    </div>
  )
}
