export function McpSetup() {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Connect ctxlayer to your AI tool</h2>
      <p style={{ color: 'var(--muted)' }}>
        OAuth (DCR) setup snippet and paste-bearer fallback arrive in M2.
      </p>
    </div>
  )
}
